# Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.lock.info

# Note: We keep terraform.tfvars because it contains our project configuration 
# (VPC CIDR, instance types, etc). Sensitive keys are handled via GitHub Secrets.
# *.tfvars 

# .NET
bin/
obj/
dist/
.vs/
.vscode/

# Ansible
*.retry

# We remove inventory.ini from ignore because the CI/CD pipeline 
# needs it as a template to inject the dynamic IP addresses.
# ansible/inventory.ini

# OS generated files
.DS_Store
Thumbs.db
desktop.ini
